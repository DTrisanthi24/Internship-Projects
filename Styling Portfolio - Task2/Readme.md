Styling Personal Portfolio Webpage


~ Project Overview
This project is a personal portfolio webpage built using HTML and CSS only.
It showcases your profile, skills, and contact information in a clean, modern design. The styling uses multi‑color animated gradients, glassmorphism cards, and responsive layouts to create an attractive and professional look.


~ Objectives
Build a visually appealing portfolio using pure HTML & CSS.

Demonstrate advanced CSS techniques such as gradients, animations, and glassmorphism.

Ensure responsiveness across devices (desktop, tablet, mobile).

Maintain accessibility and readability with high‑contrast text and clean layouts.


~ Setup and Installation
Clone or download the project folder:

bash
git clone <your-repo-link>

Ensure the following file is present:

index.html → contains both structure and styling (CSS embedded in <style>).

Open index.html in any modern browser (Chrome, Edge, Firefox).

No external dependencies are required — the project runs with pure HTML & CSS.

📂 Code Structure
Code
project-folder/
│
└── index.html   # Contains webpage structure and embedded CSS styling

Header: Title, profile image, navigation bar.

Main: Sections for About, Skills, Contact.

Footer: Copyright.

CSS (embedded):

Root variables for gradients and card styling.

Animated background using @keyframes gradientBG.

Glassmorphism effects with backdrop-filter.

Hover animations for links, buttons, and list items.

Responsive design with media queries.


🖼 Screenshots
(Add screenshots after running the project in your browser)

Homepage view with animated gradient background.

Skills section with hover effects.

Contact form with gradient button.

Mobile view showing responsive layout.


~ Technical Requirements Met

Multi‑Gradient Background: Implemented using linear-gradient and @keyframes gradientBG.

Glassmorphism Cards: Achieved with rgba transparency and backdrop-filter: blur(10px).

Hover Effects: Navigation links, skills list, and buttons scale and change color on hover.

Responsive Design: Media queries adjust font sizes, section widths, and navigation layout for smaller screens.

Custom Font: Uses Times New Roman for a formal, professional style without external imports.
